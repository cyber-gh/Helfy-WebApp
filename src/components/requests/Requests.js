import React from "react";
import utils from "../../other/utils";

const Requests = (props) => {
  return(
    <React.Fragment>
      <div>
        Hello from Requests
      </div>
    </React.Fragment>
  )
}

export default Requests;